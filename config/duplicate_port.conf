# Test config: duplicate port (should produce warning)

server {
	listen 8080;
	server_name localhost;

	location / {
		root www;
		index index.html;
		methods GET;
	}
}

server {
	listen 8080;
	server_name localhost;

	location / {
		root www;
		index index.html;
		methods GET;
	}
}
